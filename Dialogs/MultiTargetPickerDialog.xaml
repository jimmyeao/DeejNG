<Window x:Class="DeejNG.Dialogs.MultiTargetPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Applications" Height="450" Width="400"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="12"
        FontFamily="{materialDesign:MaterialDesignFont}"
        TextOptions.TextFormattingMode="Ideal">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Select applications for this slider" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                   Margin="0,0,0,10"/>

        <!-- Session List with Checkboxes -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Available sessions -->
            <GroupBox Header="Available Applications" Grid.Column="0" Margin="0,0,5,0">
                <ListBox x:Name="AvailableSessionsListBox" 
                       SelectionMode="Extended" 
                       Height="300"
                       Margin="0,5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding FriendlyName}" 
                                    IsChecked="{Binding IsSelected}" 
                                    Margin="0,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <!-- Input device selection -->
            <GroupBox Header="Input Devices" Grid.Column="1" Width="180">
                <ListBox x:Name="InputDevicesListBox" 
                       SelectionMode="Extended"
                       Height="300"
                       Margin="0,5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding FriendlyName}" 
                                    IsChecked="{Binding IsSelected}" 
                                    Margin="0,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" 
                  Width="80" 
                  Margin="0,0,10,0" 
                  Click="Cancel_Click"
                  Style="{DynamicResource MaterialDesignFlatButton}"/>
            <Button Content="OK" 
                  Width="80" 
                  Click="Ok_Click"
                  Style="{DynamicResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>