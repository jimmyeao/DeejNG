<UserControl x:Class="DeejNG.Dialogs.ChannelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="100">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" CornerRadius="6" Margin="4">
        <StackPanel>
            <Slider x:Name="VolumeSlider" Orientation="Vertical" Minimum="0" Maximum="1" Height="100" IsEnabled="False"/>
            <ProgressBar x:Name="LevelMeter" Minimum="0" Maximum="1" Height="10" Margin="0,5,0,0"/>
            <TextBox x:Name="TargetTextBox" Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
