<Window x:Class="DeejNG.Views.ChannelPickerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChannelPicker"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen">

    <Border x:Name="RootBorder"
            Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource PrimaryBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: channel name -->
            <Border Grid.Row="0"
                    Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="8,8,0,0"
                    Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ChannelNameText"
                               Grid.Column="0"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                               Text="ASSIGN TARGET"
                               FontSize="10"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0"/>
                </Grid>
            </Border>

            <!-- Category tabs -->
            <Border Grid.Row="1"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="10,6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border x:Name="AppsCategoryBorder" CornerRadius="4" Padding="14,4" Margin="2,0">
                        <TextBlock x:Name="AppsCategoryText" Text="Apps" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border x:Name="InputsCategoryBorder" CornerRadius="4" Padding="14,4" Margin="2,0">
                        <TextBlock x:Name="InputsCategoryText" Text="Inputs" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border x:Name="OutputsCategoryBorder" CornerRadius="4" Padding="14,4" Margin="2,0">
                        <TextBlock x:Name="OutputsCategoryText" Text="Outputs" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Items list (built in code-behind) -->
            <Border Grid.Row="2" Padding="8,6" MinHeight="60" MaxHeight="300">
                <ScrollViewer x:Name="ListScrollViewer"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="ItemsPanel"/>
                </ScrollViewer>
            </Border>

            <!-- Footer hint -->
            <Border Grid.Row="3"
                    Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="0,0,8,8"
                    Padding="12,6">
                <TextBlock Text="CONFIRM=select  ·  BACK=category  ·  Encoder=scroll  ·  Mute=confirm"
                           FontSize="10"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </Border>
</Window>
